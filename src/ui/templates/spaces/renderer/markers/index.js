import MarkerItem from './MarkerItem.vue';

export const components = {
  MarkerItem,
};

export const markerMap = {
  edge: MarkerItem,
  hub: MarkerItem,
  port: MarkerItem,
  // section: MapPolygon,
};
